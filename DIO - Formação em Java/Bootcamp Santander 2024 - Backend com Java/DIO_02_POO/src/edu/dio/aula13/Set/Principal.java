package edu.dio.aula13.Set;

/*
 * Não pode ser utilizada em elementos duplicados
 * Serve apenas para elementos únicos
 */

/*
 * 1. Conjunto de Convidados
 * Crie uma classe chamada "ConjuntoConvidados" que possui um conjunto de objetos do tipo "Convidado" 
 * como atributo. Cada convidado possui atributos como nome e código do convite. Implemente os seguintes 
 * métodos:
 * 
 * adicionarConvidado(String nome, int codigoConvite): Adiciona um convidado ao conjunto.
 * removerConvidadoPorCodigoConvite(int codigoConvite): Remove um convidado do conjunto com base no código do convite.
 * contarConvidados(): Conta o número total de convidados no Set.
 * exibirConvidados(): Exibe todos os convidados do conjunto.
 */

public class Principal 
{

	/*
	 * A interface Set é uma coleção que não pode conter elementos duplicados.
	 * Essa interface representa o conceito matemático de um conjunto e é usada para representar conjuntos, 
	 * como um baralho de cartas.
	 * A plataforma Java possui três implementações de Set de uso geral: HashSet, TreeSet e LinkedHashSet.
	 * A interface Set não permite acesso aleatório a um elemento na coleção.
	 * Para percorrer os elementos de um Set, você pode usar um iterador ou um loop foreach.
	 * 
	 */
}
